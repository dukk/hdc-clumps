# Copy to clumps/services/postfix-relay/.env in hdc-private (or hdc root; never commit).
# Values are optional unless the package manifest declares env_required.

# Postfix relay / SMTP2GO (`clumps/services/postfix-relay`) — smtp2go username and password (prefer vault).
# node apps/hdc-cli/cli.mjs secrets set HDC_POSTFIX_RELAY_SMTP_USER
# node apps/hdc-cli/cli.mjs secrets set HDC_POSTFIX_RELAY_SMTP_PASSWORD
# HDC_POSTFIX_RELAY_SMTP_USER=
# HDC_POSTFIX_RELAY_SMTP_PASSWORD=
