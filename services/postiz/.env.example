# Copy to clumps/services/postiz/.env in hdc-private (or hdc root; never commit).
# Values are optional unless the package manifest declares env_required.

# Postiz social scheduling (`clumps/services/postiz`) — prefer vault over env.
# Auto-generated on first deploy if missing:
# hdc secrets set HDC_POSTIZ_DB_PASSWORD
# hdc secrets set HDC_POSTIZ_JWT_SECRET
# HDC_POSTIZ_DB_PASSWORD=
# HDC_POSTIZ_JWT_SECRET=
