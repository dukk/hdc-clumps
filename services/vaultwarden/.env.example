# Copy to clumps/services/vaultwarden/.env in hdc-private (or hdc root; never commit).
# Values are optional unless the package manifest declares env_required.

# Vaultwarden (`clumps/services/vaultwarden`) — admin panel token; prefer vault.
# hdc secrets set HDC_VAULTWARDEN_ADMIN_TOKEN
# HDC_VAULTWARDEN_ADMIN_TOKEN=
