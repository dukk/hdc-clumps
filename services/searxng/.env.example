# Copy to clumps/services/searxng/.env in hdc-private (or hdc root; never commit).
# Values are optional unless the package manifest declares env_required.

# SearXNG (`clumps/services/searxng`) — instance secret (SEARXNG_SECRET); auto-generated on first deploy if missing.
# node apps/hdc-cli/cli.mjs secrets set HDC_SEARXNG_SECRET
# HDC_SEARXNG_SECRET=
