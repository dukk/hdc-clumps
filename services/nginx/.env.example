# Copy to clumps/services/nginx/.env in hdc-private (or hdc root; never commit).
# Values are optional unless the package manifest declares env_required.

# Nginx web reverse proxy (`clumps/services/nginx`) — prefer vault over env.
# node apps/hdc-cli/cli.mjs secrets set HDC_NGINX_LE_EMAIL
# HDC_NGINX_LE_EMAIL=
