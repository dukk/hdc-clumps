# Copy to clumps/services/greenbone/.env in hdc-private (or hdc root; never commit).
# Greenbone Community Edition — values optional unless manifest env_required is set.
# Prefer vault: node apps/hdc-cli/cli.mjs secrets set <KEY>

# HDC_GREENBONE_ADMIN_PASSWORD=
