# Copy to clumps/services/rustfs/.env in hdc-private (or hdc root; never commit).
# RustFS S3 object storage cluster — values optional unless manifest env_required is set.
# Prefer vault: node apps/hdc-cli/cli.mjs secrets set <KEY>

# HDC_RUSTFS_ACCESS_KEY=
# HDC_RUSTFS_SECRET_KEY=
