# Copy to clumps/services/bind/.env in hdc-private (or hdc root; never commit).
# BIND DNS — values optional unless manifest env_required is set.
# Prefer vault: hdc secrets set <KEY>

# HDC_BIND_TSIG_KEY=
