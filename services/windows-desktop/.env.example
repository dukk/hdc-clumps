# Copy to clumps/services/windows-desktop/.env in hdc-private (or hdc root; never commit).
# Windows 11 desktop (Proxmox QEMU) — values optional unless manifest env_required is set.
# Prefer vault: node apps/hdc-cli/cli.mjs secrets set <KEY>

# HDC_WINDOWS_DESKTOP_ADMIN_PASSWORD=
