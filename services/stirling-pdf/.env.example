# Copy to clumps/services/stirling-pdf/.env in hdc-private (or hdc root; never commit).
# Values are optional unless the package manifest declares env_required.

# Stirling PDF (`clumps/services/stirling-pdf`) — initial admin login password.
# hdc secrets set HDC_STIRLING_PDF_ADMIN_PASSWORD
# HDC_STIRLING_PDF_ADMIN_PASSWORD=
