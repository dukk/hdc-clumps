# Copy to clumps/services/kali-desktop/.env in hdc-private (or hdc root; never commit).
# Kali Linux desktop (Proxmox QEMU) — values optional unless manifest env_required is set.
# Prefer vault: hdc secrets set <KEY>

# HDC_KALI_DESKTOP_PASSWORD=
