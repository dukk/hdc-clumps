# Copy to clumps/services/wallabag/.env in hdc-private (or hdc root; never commit).
# Wallabag read-it-later — values optional unless manifest env_required is set.
# Prefer vault: node apps/hdc-cli/cli.mjs secrets set <KEY>

# HDC_WALLABAG_DB_PASSWORD=
# HDC_WALLABAG_SECRET=
