# Copy to clumps/services/apprise/.env in hdc-private (or hdc root; never commit).
# Apprise notification API — values optional unless manifest env_required is set.
# Prefer vault: hdc secrets set <KEY>

# Optional Django SECRET_KEY for hash-mode config storage (simple mode does not require it).
# HDC_APPRISE_SECRET_KEY=
