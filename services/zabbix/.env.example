# Copy to clumps/services/zabbix/.env in hdc-private (or hdc root; never commit).
# Zabbix monitoring stack — values optional unless manifest env_required is set.
# Prefer vault: hdc secrets set <KEY>

# HDC_ZABBIX_DB_PASSWORD=
