# Copy to clumps/services/redis/.env in hdc-private (or hdc root; never commit).
# Values are optional unless the package manifest declares env_required.

# Redis Cluster (`clumps/services/redis`) — prefer vault over env.
# hdc secrets set HDC_REDIS_PASSWORD
# HDC_REDIS_PASSWORD=
