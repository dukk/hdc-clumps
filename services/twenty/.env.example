# Copy to clumps/services/twenty/.env in hdc-private (or hdc root; never commit).
# Twenty CRM — values optional unless manifest env_required is set.
# Prefer vault: node apps/hdc-cli/cli.mjs secrets set <KEY>

# HDC_TWENTY_DB_PASSWORD=
# HDC_TWENTY_ENCRYPTION_KEY=
# HDC_TWENTY_ENCRYPTION_KEY_FALLBACK=  # optional — set only during intentional ENCRYPTION_KEY rotation
