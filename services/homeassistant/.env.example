# Copy to clumps/services/homeassistant/.env in hdc-private (or hdc root; never commit).
# Prefer vault: hdc secrets set <KEY>

# Long-lived Home Assistant access token (Settings → People → Long-lived access tokens).
# Used by: query --import
# HDC_HOMEASSISTANT_TOKEN=  (vault only; do not put the value here)
