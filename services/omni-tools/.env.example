# Copy to clumps/services/omni-tools/.env in hdc-private (or hdc root; never commit).
# OmniTools web utilities — values optional unless manifest env_required is set.
# Prefer vault: hdc secrets set <KEY>

# No documented HDC_* env vars for this package (vault-only or config.json).
