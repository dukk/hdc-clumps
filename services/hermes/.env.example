# Copy to clumps/services/hermes/.env in hdc-private (or hdc root; never commit).
# Hermes Agent — values optional unless manifest env_required is set.
# Prefer vault: node apps/hdc-cli/cli.mjs secrets set <KEY>

# HDC_HERMES_DASHBOARD_PASSWORD=
# HDC_HERMES_DISCORD_BOT_TOKEN=
# HDC_HERMES_OPENROUTER_API_KEY=
