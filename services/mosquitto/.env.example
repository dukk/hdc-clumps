# Copy to clumps/services/mosquitto/.env in hdc-private (or hdc root; never commit).
# Mosquitto MQTT broker — values optional unless manifest env_required is set.
# Prefer vault: node apps/hdc-cli/cli.mjs secrets set <KEY>

# No documented HDC_* env vars for this package (vault-only or config.json).
