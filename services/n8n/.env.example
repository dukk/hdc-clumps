# Copy to clumps/services/n8n/.env in hdc-private (or hdc root; never commit).
# Values are optional unless the package manifest declares env_required.

# n8n (`clumps/services/n8n`) — credential encryption key; auto-generated on first deploy if missing.
# hdc secrets set HDC_N8N_ENCRYPTION_KEY
# HDC_N8N_ENCRYPTION_KEY=
