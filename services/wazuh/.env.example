# Copy to clumps/services/wazuh/.env in hdc-private (or hdc root; never commit).
# Wazuh single-node stack — values optional unless manifest env_required is set.
# Prefer vault: hdc secrets set <KEY>

# HDC_WAZUH_AGENT_PASSWORD=
# HDC_WAZUH_API_PASSWORD=
