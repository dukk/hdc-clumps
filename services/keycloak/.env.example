# Copy to clumps/services/keycloak/.env in hdc-private (or hdc root; never commit).
# Keycloak identity and access management — values optional unless manifest env_required is set.
# Prefer vault: node apps/hdc-cli/cli.mjs secrets set <KEY>

# HDC_KEYCLOAK_ADMIN_USER=hdc
# HDC_KEYCLOAK_ADMIN_PASSWORD=
