# Copy to clumps/services/step-ca/.env in hdc-private (or hdc root; never commit).
# Values are optional unless the package manifest declares env_required.

# Smallstep step-ca internal CA (`clumps/services/step-ca`) — prefer vault over env.
# node apps/hdc-cli/cli.mjs secrets set HDC_STEP_CA_PASSWORD
# Optional per-instance: HDC_STEP_CA_PASSWORD_A
# HDC_STEP_CA_PASSWORD=
