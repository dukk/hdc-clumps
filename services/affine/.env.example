# Copy to clumps/services/affine/.env in hdc-private (or hdc root; never commit).
# AFFiNE workspace — values optional unless manifest env_required is set.
# Prefer vault: node apps/hdc-cli/cli.mjs secrets set <KEY>

# HDC_AFFINE_DB_PASSWORD=
# Copilot (when affine.copilot.enabled): reuse LiteLLM master key or a virtual key
# HDC_LITELLM_MASTER_KEY=
