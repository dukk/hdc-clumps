# Copy to clumps/services/gatus/.env in hdc-private (or hdc root; never commit).
# Values are optional unless the package manifest declares env_required.

# Gatus health dashboard (`clumps/services/gatus`) — optional alerting tokens in config_yaml_extra via ${ENV}.
# Example: hdc secrets set HDC_GATUS_SLACK_WEBHOOK
# HDC_GATUS_SLACK_WEBHOOK=
