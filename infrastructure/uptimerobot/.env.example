# Copy to clumps/infrastructure/uptimerobot/.env in hdc-private (or hdc root; never commit).
# Values are optional unless the package manifest declares env_required.

# UptimeRobot API (`clumps/infrastructure/uptimerobot`) — monitors, status pages, alert contacts.
# node apps/hdc-cli/cli.mjs secrets set HDC_UPTIMEROBOT_API_KEY
# HDC_UPTIMEROBOT_API_KEY=
