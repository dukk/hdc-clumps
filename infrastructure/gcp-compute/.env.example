# Copy to clumps/infrastructure/gcp-compute/.env in hdc-private (or hdc root; never commit).
# GCP compute — values optional unless manifest env_required is set.
# Prefer vault: node apps/hdc-cli/cli.mjs secrets set <KEY>

# HDC_GCP_COMPUTE_PROJECT_ID=
