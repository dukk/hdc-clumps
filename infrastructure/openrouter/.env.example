# Copy to clumps/infrastructure/openrouter/.env in hdc-private (or hdc root; never commit).
# Values are optional unless the package manifest declares env_required.

# OpenRouter Management API (`clumps/infrastructure/openrouter`) — credits and key admin; not for chat completions.
# Inference keys for consumers stay separate (e.g. HDC_HERMES_OPENROUTER_API_KEY for hermes).
# Hermes also accepts HDC_OPENROUTER_API_KEY as a shared fallback when HDC_HERMES_OPENROUTER_API_KEY is unset.
# node apps/hdc-cli/cli.mjs secrets set HDC_OPENROUTER_MANAGEMENT_API_KEY
# HDC_OPENROUTER_MANAGEMENT_API_KEY=
# node apps/hdc-cli/cli.mjs secrets set HDC_HERMES_OPENROUTER_API_KEY
# HDC_HERMES_OPENROUTER_API_KEY=
# node apps/hdc-cli/cli.mjs secrets set HDC_OPENROUTER_API_KEY
# HDC_OPENROUTER_API_KEY=
