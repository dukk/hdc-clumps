# Copy to clumps/infrastructure/proxmox/.env in hdc-private (or hdc root; never commit).
# Values are optional unless the package manifest declares env_required.

#   clumps/infrastructure/proxmox/.env.example
#   clumps/services/homepage/.env.example
